[package]
name = "rust"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false
rust-version = "1.85"

[lib]
crate-type = ["staticlib"]

[profile.release]
debug = 1
lto = "thin"

[dependencies]
roxido = { path = "roxido" }
vodozemac = "0.10"
base64 = "0.22"
